export default function Disclaimer() {
  return (
    <div className="disclaimer">
      Parcel boundaries and property data are approximate for general reference only, not a legal survey, title opinion,
      zoning determination, or substitute for county/municipal verification.
    </div>
  );
}
